*** Settings ***
Documentation     NOTE:
...               add note here
Suite Setup       Suite Setup
Force Tags        INSTALL_MSI    win
Test Template     Install Msi Template
Resource          ../../resources/keywords.txt

*** Test Cases ***    NAME                         PATH               RETURN CODE    RETURN STATUS
Fresh Install Firefox
                      FirefoxESR-31.3.0-ach.msi    ${_WORKING_DIR}    0              ${ENDED_OK - ended normally}

Fresh Install Not Exist
                      NotExist.msi                 ${_WORKING_DIR}    0              FAULT_POST_PROCESSING - error in post processing.

*** Keywords ***
Install Msi Template
    [Arguments]    ${name}    ${path}    ${returnCode}    ${returnStatus}
    Init Action    ${ACTION_UNINSTALL_MSI}
    Action Set    ${PRT_UNINSTALL_MSI_PACKAGE_NAME}    ${name}
    Action Set    ${PRT_UNINSTALL_MSI_PACKAGE_PATH}    ${path}
    Action Set Timeout    600
    Action Execute
    Init Action    ${ACTION_INSTALL_MSI}
    Action Set    ${PRT_INSTALL_MSI_PACKAGE_NAME}    ${name}
    Action Set    ${PRT_INSTALL_MSI_PACKAGE_PATH}    ${path}
    Action Set    ${PRT_INSTALL_MSI_ADMIN_INSTALL}    NO
    Action Execute
    Action Report Should Be Found
    Action Return Code Should Be    ${returnCode}
    Action Return Status Should Be    ${returnStatus}
